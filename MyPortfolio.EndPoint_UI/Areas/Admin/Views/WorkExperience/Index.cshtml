@model MyPortfolio.Application.ViewModels.PagedListViewModel<MyPortfolio.Application.ViewModels.WorkExperienceViewModel>


@{
    ViewData["Title"] = "مدیریت تجربه های کاری";
}

<div class="container mt-5" dir="rtl">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold">مدیریت  تجربه ها</h2>

        <a asp-controller="WorkExperience"
           asp-action="Create"
           class="btn btn-success px-4">
            + افزودن تجربه جدید
        </a>
    </div>

    @if (Model == null || !Model.Items.Any())
    {
        <div class="alert alert-info text-center p-4">
            هنوز هیچ تجربه ای ثبت نشده است.
        </div>
    }
    else
    {
        <table class="table table-striped table-hover text-center align-middle shadow">
            <thead class="table-dark">
                <tr>
                    <th>عنوان تجربه به فارسی</th>
                    <th>عنوان تجربه به انگلیسی</th>

                    <th>اسم شرکت به فارسی</th>
                    <th>اسم شرکت به انگلیسی</th>

                    <th>تاریخ شروع</th>
                    <th>تاریخ پایان</th>
                    <th> به فارسی توضیحات</th>
                    <th>توضیحات به انگلیسی</th>

                    <th style="width:140px;">عملیات</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var exprience in Model.Items)
                {
                    <tr>
                        <td>@exprience.TitleFa</td>
                        <td>@exprience.TitleEn</td>

                        <td>@exprience.CompanyNameFa</td>
                        <td>@exprience.CompanyNameEn</td>

                        <td>@exprience.DateOfStart</td>
                        <td>@exprience.DateOfEnd</td>
                        <td>@exprience.DescriptionFa</td>
                        <td>@exprience.DescriptionEn</td>





                        <td>
                            <a asp-controller="WorkExperience"
                               asp-action="Edit"
                               asp-route-id="@exprience.Id"
                               class="btn btn-warning btn-sm">
                                ویرایش
                            </a>

                            <form asp-controller="WorkExperience"
                                  asp-action="Delete"
                                  asp-route-id="@exprience.Id"
                                  method="post"
                                  style="display:inline">

                                <button type="submit"
                                        class="btn btn-danger btn-sm">
                                    حذف
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="pagination">
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <a class="@(i == Model.CurrentPage ? "active" : "")"
                   href="@Url.Action("Index", new { page = i })">
                    @i
                </a>
            }
        </div>

    }

   
    <script>
        document.addEventListener("click", async function (e) {

            if (!e.target.classList.contains("btn-delete"))
                return;

            const id = e.target.getAttribute("data-id");

            if (!confirm("آیا مطمئن هستید که می‌خواهید این تجربه را حذف کنید؟"))
                return;

            const response = await fetch('/WorkExperience/Delete', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ id: id })
            });

            const result = await response.json();

            if (result.success) {
                alert("تجربه با موفقیت حذف شد");


                const item = e.target.closest(".WorkExperience-item");
                if (item) {
                    item.remove();
                }
            } else {
                alert(result.message ?? "خطایی رخ داد");
            }
        });
    </script>


</div>

